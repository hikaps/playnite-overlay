<UserControl x:Class="PlayniteOverlay.OverlaySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:PlayniteOverlay.Models"
             xmlns:converters="clr-namespace:PlayniteOverlay.Converters"
             mc:Ignorable="d"
             d:DesignWidth="540" d:DesignHeight="320">
    <UserControl.Resources>
        <converters:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter" />
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="10" Orientation="Vertical" >
            <TextBlock Text="Overlay Settings" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <GroupBox Header="Keyboard Shortcut">
                <StackPanel Margin="10">
                    <TextBlock Text="Set a custom global hotkey to toggle the overlay from anywhere." TextWrapping="Wrap"/>
                    <CheckBox Content="Enable custom global hotkey"
                              Margin="0,8,0,0"
                              IsChecked="{Binding Settings.EnableCustomHotkey}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="Custom hotkey:" VerticalAlignment="Center"/>
                        <TextBox Width="200"
                                 Margin="8,0,0,0"
                                 Text="{Binding Settings.CustomHotkey}"
                                 IsReadOnly="True"
                                 PreviewKeyDown="Hotkey_PreviewKeyDown"
                                 ToolTip="Click here and press the desired key combo"/>
                        <Button Margin="8,0,0,0" Content="Clear" Click="Hotkey_Clear_Click"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Controller">
                <StackPanel Margin="10">
                    <CheckBox Content="Allow opening overlay with controller"
                              IsChecked="{Binding Settings.UseControllerToOpen}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="Button combo:" VerticalAlignment="Center"/>
                        <ComboBox Width="160" Margin="8,0,0,0"
                                  SelectedValuePath="Content"
                                  SelectedValue="{Binding Settings.ControllerCombo}">
                            <ComboBoxItem Content="Guide" />
                            <ComboBoxItem Content="Start+Back" />
                            <ComboBoxItem Content="LB+RB" />
                        </ComboBox>
                    </StackPanel>
                    <CheckBox Content="Controller input always active (not just during gameplay)"
                              Margin="0,8,0,0"
                              IsChecked="{Binding Settings.ControllerAlwaysActive}"
                              ToolTip="Warning: May conflict with system Xbox overlay when enabled"/>
                    <CheckBox Content="Only enable controller for PC games"
                              Margin="0,8,0,0"
                              IsChecked="{Binding Settings.PcGamesOnly}"
                              ToolTip="When enabled, controller input is disabled for emulated/console games. Useful when emulators have their own overlays."/>
                    <TextBlock Margin="0,6,0,0" TextWrapping="Wrap"
                               Foreground="Gray"
                               Text="Note: Controller detection uses XInput and may not work with all devices yet."/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Running Apps Detection">
                <StackPanel Margin="10">
                    <CheckBox Content="Show non-game applications (browsers, editors, etc.)"
                              IsChecked="{Binding Settings.ShowGenericApps}"
                              ToolTip="When enabled, shows all running apps with visible windows. When disabled, only shows games."/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="Maximum apps to display:" VerticalAlignment="Center"/>
                        <TextBox Width="60" Margin="8,0,0,0"
                                 Text="{Binding Settings.MaxRunningApps, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Maximum number of running apps to show in the overlay (1-50)"/>
                    </StackPanel>
                    <CheckBox Content="Show notifications"
                              Margin="0,8,0,0"
                              IsChecked="{Binding Settings.ShowNotifications}"
                              ToolTip="When enabled, shows notifications for app switching, exit operations, and errors."/>
                    <TextBlock Margin="0,6,0,0" TextWrapping="Wrap"
                               Foreground="Gray"
                               Text="The overlay can detect and switch between multiple running games and applications."/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Achievements (SuccessStory)">
                <StackPanel Margin="10">
                    <CheckBox Content="Show achievement progress in overlay"
                              IsChecked="{Binding Settings.ShowAchievements}"
                              ToolTip="Display achievement progress when SuccessStory plugin is installed and game has achievement data."/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="Recent unlocked to show:" VerticalAlignment="Center"/>
                        <TextBox Width="60" Margin="8,0,0,0"
                                 Text="{Binding Settings.MaxRecentAchievements, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Maximum number of recently unlocked achievements to display (1-10)"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="Locked achievements to show:" VerticalAlignment="Center"/>
                        <TextBox Width="60" Margin="8,0,0,0"
                                 Text="{Binding Settings.MaxLockedAchievements, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Maximum number of locked achievements to display (1-10)"/>
                    </StackPanel>
                    <TextBlock Margin="0,6,0,0" TextWrapping="Wrap"
                               Foreground="Gray"
                               Text="Requires the SuccessStory plugin to be installed. Achievement data is read from SuccessStory's cache."/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Fullscreen Compatibility">
                <StackPanel Margin="10">
                    <CheckBox Content="Force borderless windowed mode for games"
                              IsChecked="{Binding Settings.ForceBorderlessMode}"
                              ToolTip="Automatically converts windowed games to borderless fullscreen. This helps the overlay work on games that don't natively support it."/>
                    <TextBlock Margin="0,6,0,0" TextWrapping="Wrap"
                               Foreground="Gray"
                               Text="When enabled, games running in windowed mode will be converted to borderless fullscreen after launch. This allows the overlay to appear over the game."/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="Delay before applying (ms):" VerticalAlignment="Center"/>
                        <TextBox Width="60" Margin="8,0,0,0"
                                 Text="{Binding Settings.BorderlessDelayMs, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Time to wait after game starts before applying borderless mode (1000-10000 ms)"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Focus &amp; Input Control">
                <StackPanel Margin="10">
                    <CheckBox Content="Suspend game while overlay is open"
                              IsChecked="{Binding Settings.SuspendGameOnOverlay}"
                              ToolTip="Freezes the game process while the overlay is open, preventing it from stealing focus."/>
                    <TextBlock Margin="0,8,0,0" TextWrapping="Wrap"
                               Foreground="Gray"
                               Text="When enabled, the game process is suspended when the overlay opens and resumed when it closes. This prevents games from stealing focus back from the overlay."/>
                    <TextBlock Margin="0,8,0,0" TextWrapping="Wrap"
                               Foreground="Orange"
                               FontWeight="Bold"
                               Text="⚠️ WARNING: May trigger anti-cheat detection!"
                               ToolTip="Process suspension can be detected by anti-cheat systems like Easy Anti-Cheat, BattlEye, etc. Use at your own risk, especially in online multiplayer games."/>
                    <TextBlock Margin="0,4,0,0" TextWrapping="Wrap"
                               Foreground="Orange"
                               Text="Recommended for single-player games only. Disable for competitive/online games."/>
                </StackPanel>
            </GroupBox>
            <GroupBox Header="Shortcuts">
                <StackPanel Margin="10">
                    <TextBlock Text="Configure custom shortcut buttons that appear in the overlay." TextWrapping="Wrap" Margin="0,0,0,10"/>
                    <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="ShortcutsList" ItemsSource="{Binding Settings.Shortcuts}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Expander x:Name="ShortcutExpander" Margin="0,2,0,2"
                                              Expanded="ShortcutExpander_Expanded">
                                        <Expander.Header>
                                            <Grid Margin="4,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="120"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding Label}" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" TextTrimming="None" TextWrapping="Wrap"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Command}" Foreground="{DynamicResource TextBrush}" TextTrimming="None" TextWrapping="Wrap" Margin="8,0,0,0"/>
                                            </Grid>
                                        </Expander.Header>
                                        <Grid Margin="5,5,5,5">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="70"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Label:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                            <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
                                                     Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}"
                                                     Margin="0,0,0,6"
                                                     Foreground="{DynamicResource TextBrush}"/>

                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Action:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                            <ComboBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,6"
                                                      SelectedValuePath="Tag"
                                                      SelectedValue="{Binding ActionType}"
                                                      Foreground="{DynamicResource TextBrush}">
                                                <ComboBoxItem Content="CommandLine" Tag="{x:Static models:ShortcutActionType.CommandLine}"/>
                                                <ComboBoxItem Content="SendInput" Tag="{x:Static models:ShortcutActionType.SendInput}"/>
                                            </ComboBox>

                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Command:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"
                                                       Visibility="{Binding ActionType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static models:ShortcutActionType.CommandLine}}"/>
                                            <TextBox Grid.Row="2" Grid.Column="1"
                                                     Text="{Binding Command, UpdateSourceTrigger=PropertyChanged}"
                                                     Margin="0,0,0,6"
                                                     Foreground="{DynamicResource TextBrush}"
                                                     Visibility="{Binding ActionType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static models:ShortcutActionType.CommandLine}}"/>
                                            <Button Grid.Row="2" Grid.Column="2" Content="Browse"
                                                    MinWidth="70" Margin="5,0,0,6"
                                                    Click="ShortcutBrowse_Click"
                                                    Tag="{Binding}"
                                                    Foreground="{DynamicResource TextBrush}"
                                                    Visibility="{Binding ActionType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static models:ShortcutActionType.CommandLine}}"/>

                                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Arguments:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"
                                                       Visibility="{Binding ActionType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static models:ShortcutActionType.CommandLine}}"/>
                                            <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2"
                                                     Text="{Binding Arguments, UpdateSourceTrigger=PropertyChanged}"
                                                     Margin="0,0,0,6"
                                                     Foreground="{DynamicResource TextBrush}"
                                                     Visibility="{Binding ActionType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static models:ShortcutActionType.CommandLine}}"/>

                                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Hotkey:" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"
                                                       Visibility="{Binding ActionType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static models:ShortcutActionType.SendInput}}"/>
                                            <Grid Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,6"
                                                  Visibility="{Binding ActionType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static models:ShortcutActionType.SendInput}}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBox Grid.Column="0"
                                                         Text="{Binding Hotkey}"
                                                         IsReadOnly="True"
                                                         PreviewKeyDown="ShortcutHotkey_PreviewKeyDown"
                                                         Tag="{Binding}"
                                                         Foreground="{DynamicResource TextBrush}"/>
                                                <Button Grid.Column="1" Content="Clear" Margin="6,0,0,0"
                                                        Click="ShortcutHotkey_Clear_Click"
                                                        Tag="{Binding}"
                                                        Foreground="{DynamicResource TextBrush}"/>
                                            </Grid>

                                            <Button Grid.Row="5" Grid.Column="2" Content="Delete"
                                                    MinWidth="70" HorizontalAlignment="Right"
                                                    Click="ShortcutDelete_Click"
                                                    Tag="{Binding}"
                                                    Foreground="{DynamicResource TextBrush}"/>
                                        </Grid>
                                    </Expander>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <Button x:Name="AddShortcutBtn" Content="+ Add Shortcut"
                            HorizontalAlignment="Left" Margin="0,10,0,0"
                            Click="AddShortcutBtn_Click"/>
                    <TextBlock Text="Maximum 10 shortcuts" FontSize="10" Foreground="Gray" Margin="0,5,0,0"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>

<Window x:Class="PlayniteOverlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#B3000000"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        WindowState="Maximized"
        ResizeMode="NoResize"
        Focusable="True">
    <Grid x:Name="Backdrop" Background="Transparent">
        <Border x:Name="RootCard" CornerRadius="10" Background="#EE1A1A1A" 
                Padding="28" HorizontalAlignment="Center" VerticalAlignment="Center" 
                Width="920" MaxHeight="620" Opacity="0">
            <ScrollViewer x:Name="ContentScroller" 
                          VerticalScrollBarVisibility="Hidden" 
                          HorizontalScrollBarVisibility="Disabled"
                          Focusable="False">
                <StackPanel>
                    <!-- Header -->
                    <TextBlock Text="Playnite Overlay" FontSize="24" FontWeight="SemiBold" 
                               Foreground="#EEEEEE" Margin="0,0,0,20"/>
                    
                    <!-- Current Game Section (collapsed when no game running) -->
                    <Border x:Name="CurrentGameSection" Background="#252525" CornerRadius="8" 
                            Padding="20" Margin="0,0,0,20" Visibility="Collapsed">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Cover Image -->
                            <Border Grid.Column="0" Background="#1A1A1A" CornerRadius="6" 
                                    Width="120" Height="160" HorizontalAlignment="Left" VerticalAlignment="Top">
                                <Image x:Name="CurrentGameCover" Stretch="UniformToFill" ClipToBounds="True"/>
                            </Border>
                            
                            <!-- Game Info -->
                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock Text="NOW PLAYING" FontSize="10" FontWeight="Bold" 
                                           Foreground="#888888" Margin="0,0,0,10"/>
                                <TextBlock x:Name="CurrentGameTitle" FontSize="22" 
                                           FontWeight="SemiBold" Foreground="#FFFFFF" 
                                           TextTrimming="CharacterEllipsis" Margin="0,0,0,6" TextWrapping="NoWrap"/>
                                <TextBlock x:Name="CurrentGameInfo" FontSize="13" 
                                           Foreground="#AAAAAA" Margin="0,0,0,20" TextWrapping="Wrap"/>
                                
                                <!-- Action Buttons -->
                                <StackPanel Orientation="Horizontal">
                                    <Button x:Name="SwitchBtn" Content="Return to Playnite" 
                                            Margin="0,0,12,0" Padding="16,8" MinWidth="150"
                                            Background="#3A3A3A" Foreground="White" BorderThickness="0"
                                            FontSize="13" FontWeight="Medium" Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="ButtonBorder"
                                                                    Background="{TemplateBinding Background}" 
                                                                    BorderBrush="Transparent"
                                                                    BorderThickness="2"
                                                                    CornerRadius="5" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#4A4A4A"/>
                                                                </Trigger>
                                                                <Trigger Property="IsPressed" Value="True">
                                                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#2A2A2A"/>
                                                                </Trigger>
                                                                <Trigger Property="IsFocused" Value="True">
                                                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#FFFFFF"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    <Button x:Name="ExitBtn" Content="Exit Game" 
                                            Padding="16,8" MinWidth="110"
                                            Background="#D32F2F" Foreground="White" BorderThickness="0"
                                            FontSize="13" FontWeight="Medium" Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="ButtonBorder"
                                                                    Background="{TemplateBinding Background}" 
                                                                    BorderBrush="Transparent"
                                                                    BorderThickness="2"
                                                                    CornerRadius="5" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#E53935"/>
                                                                </Trigger>
                                                                <Trigger Property="IsPressed" Value="True">
                                                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#C62828"/>
                                                                </Trigger>
                                                                <Trigger Property="IsFocused" Value="True">
                                                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#FFFFFF"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <!-- Running Apps Section (shown when other apps detected) -->
                    <Border x:Name="RunningAppsSection" Background="#252525" CornerRadius="8" 
                            Padding="16" Margin="0,0,0,20" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="RUNNING APPS" FontSize="10" FontWeight="Bold" 
                                       Foreground="#888888" Margin="0,0,0,10"/>
                            
                            <ListBox x:Name="RunningAppsList" Background="Transparent" 
                                     BorderThickness="0" HorizontalContentAlignment="Stretch"
                                     ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="Margin" Value="0,0,0,6"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border x:Name="Container" CornerRadius="5" 
                                                            Background="#2A2A2A" Padding="10">
                                                        <ContentPresenter />
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="Container" Property="Background" Value="#353535"/>
                                                            <Setter TargetName="Container" Property="BorderBrush" Value="#6A4FC8"/>
                                                            <Setter TargetName="Container" Property="BorderThickness" Value="2"/>
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Container" Property="Background" Value="#333333"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Small icon/thumbnail -->
                                            <Border Grid.Column="0" Background="#1A1A1A" 
                                                    CornerRadius="4" Width="32" Height="32" 
                                                    Margin="0,0,12,0">
                                                <Grid>
                                                    <Image Source="{Binding ImagePath}" 
                                                           Stretch="UniformToFill" ClipToBounds="True"/>
                                                    <!-- Fallback icon -->
                                                    <TextBlock x:Name="NoAppImage" Text="🎮" FontSize="16" 
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                                               Foreground="#555555" Visibility="Collapsed"/>
                                                </Grid>
                                            </Border>
                                            
                                            <!-- App title -->
                                            <TextBlock Grid.Column="1" Text="{Binding Title}" 
                                                       FontSize="13" Foreground="#EEEEEE" FontWeight="Medium"
                                                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                            
                                            <!-- Switch button -->
                                            <Button Grid.Column="2" Content="↗ Switch" Width="90" 
                                                    Height="30" VerticalAlignment="Center" 
                                                    CommandParameter="{Binding}"
                                                    Background="#6A4FC8" Foreground="White" BorderThickness="0"
                                                    FontSize="12" FontWeight="Medium" Cursor="Hand">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border x:Name="ButtonBorder"
                                                                            Background="{TemplateBinding Background}" 
                                                                            BorderBrush="Transparent"
                                                                            BorderThickness="2"
                                                                            CornerRadius="5" Padding="{TemplateBinding Padding}">
                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter TargetName="ButtonBorder" Property="Background" Value="#7D5FDB"/>
                                                                        </Trigger>
                                                                        <Trigger Property="IsPressed" Value="True">
                                                                            <Setter TargetName="ButtonBorder" Property="Background" Value="#5A3FB5"/>
                                                                        </Trigger>
                                                                        <Trigger Property="IsFocused" Value="True">
                                                                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#FFFFFF"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </Grid>
                                        <DataTemplate.Triggers>
                                            <!-- Show fallback icon if ImagePath is null -->
                                            <DataTrigger Binding="{Binding ImagePath}" Value="{x:Null}">
                                                <Setter TargetName="NoAppImage" Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </Border>
                    
                    <!-- Recent Games Section -->
                    <StackPanel>
                        <TextBlock Text="RECENT GAMES" FontSize="10" FontWeight="Bold" 
                                   Foreground="#888888" Margin="0,0,0,10"/>
                        
                        <!-- Empty State (shown when no items) -->
                        <Border x:Name="EmptyState" Background="#252525" CornerRadius="6" 
                                Padding="40,32" Visibility="Collapsed">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="📚" FontSize="32" HorizontalAlignment="Center" 
                                           Margin="0,0,0,12" Opacity="0.5"/>
                                <TextBlock Text="No recent games" FontSize="15" 
                                           Foreground="#BBBBBB" HorizontalAlignment="Center" 
                                           Margin="0,0,0,8" FontWeight="Medium"/>
                                <TextBlock Text="Launch a game to see it here" 
                                           FontSize="12" Foreground="#777777" 
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Recent Games List -->
                        <ListBox x:Name="RecentList" Background="Transparent" 
                                 BorderThickness="0" HorizontalContentAlignment="Stretch" 
                                 Focusable="False"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0,0,0,8"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="Container" CornerRadius="6" 
                                                        Background="#252525" Padding="12">
                                                    <ContentPresenter />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Container" Property="Background" Value="#303030"/>
                                                        <Setter TargetName="Container" Property="BorderBrush" Value="#4A90E2"/>
                                                        <Setter TargetName="Container" Property="BorderThickness" Value="2"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Container" Property="Background" Value="#2A2A2A"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Small thumbnail -->
                                        <Border Grid.Column="0" Background="#1A1A1A" 
                                                CornerRadius="4" Width="50" Height="68" 
                                                Margin="0,0,14,0">
                                            <Grid>
                                                <Image Source="{Binding ImagePath}" 
                                                       Stretch="UniformToFill" ClipToBounds="True"/>
                                                <!-- Fallback text if no image -->
                                                <TextBlock x:Name="NoImageText" Text="🎮" FontSize="24" 
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                                           Foreground="#555555" Visibility="Collapsed"/>
                                            </Grid>
                                        </Border>
                                        
                                        <!-- Game info -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Title}" FontSize="14" 
                                                       Foreground="#EEEEEE" FontWeight="Medium" 
                                                       TextTrimming="CharacterEllipsis" Margin="0,0,0,4"/>
                                            <TextBlock Text="{Binding SecondaryText}" 
                                                       FontSize="11" Foreground="#888888"/>
                                        </StackPanel>
                                        
                                        <!-- Play button -->
                                        <Button Grid.Column="2" Content="▶ Play" Width="90" 
                                                Height="34" VerticalAlignment="Center" 
                                                CommandParameter="{Binding}"
                                                Background="#4A90E2" Foreground="White" BorderThickness="0"
                                                FontSize="12" FontWeight="Medium" Cursor="Hand">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border x:Name="ButtonBorder"
                                                                        Background="{TemplateBinding Background}" 
                                                                        BorderBrush="Transparent"
                                                                        BorderThickness="2"
                                                                        CornerRadius="5" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#5BA3F5"/>
                                                                    </Trigger>
                                                                    <Trigger Property="IsPressed" Value="True">
                                                                        <Setter TargetName="ButtonBorder" Property="Background" Value="#3A7BC8"/>
                                                                    </Trigger>
                                                                    <Trigger Property="IsFocused" Value="True">
                                                                        <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#FFFFFF"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </Grid>
                                    <DataTemplate.Triggers>
                                        <!-- Show fallback icon if ImagePath is null -->
                                        <DataTrigger Binding="{Binding ImagePath}" Value="{x:Null}">
                                            <Setter TargetName="NoImageText" Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
